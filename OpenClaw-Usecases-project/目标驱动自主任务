# 目标驱动自主任务

你的 AI 智能体功能强大但只是被动响应——只有在你告诉它该做什么时它才工作。如果它知道你的目标，每天主动提出能够让你更接近这些任务，而不需要你要求呢？

这个工作流将 OpenClaw 变成一个自主的员工。你只需一次头脑风暴整理所有目标，然后智能体会自主生成、安排并完成推进这些目标的任务——包括在你睡觉时为你构建意想不到的小型应用。

## 功能描述

- 你将所有目标、使命和目标输入 OpenClaw（包括个人和职业目标）
- 每天早上，智能体生成 4-5 个它可以自主在你的计算机上完成的任务
- 任务不仅限于应用构建：研究、编写脚本、构建功能、创建内容、分析竞争对手
- 智能体自行执行任务，并在为你构建的自定义看板上跟踪这些任务
- 你还可以让它每晚构建一个意想不到的小型应用——一个新的 SaaS 想法，一个自动化你生活中枯燥部分工具，以 MVP 形式交付

## 痛点

大多数人有宏伟目标但难以将其分解为每日可行的步骤。即使做到了，执行也占用了他们所有时间。这个系统将规划和执行都外包给你的 AI 智能体。你定义目的地，智能体找出每日步骤并执行它们。

## 所需技能

- Telegram 或 Discord 集成
- `sessions_spawn` / `sessions_send` 用于自主任务执行
- Next.js 或类似工具（用于看板——OpenClaw 会为你构建）

## 如何设置

### 第 1 步：头脑风暴你的目标

这是最重要的一步。向你的 OpenClaw 发送你正在尝试完成的所有目标：

```text
以下是我的目标和使命。记住所有这些内容：

职业目标：
- 将我的 YouTube 频道发展到 10 万订阅者
- 在第三季度推出我的 SaaS 产品
- 围绕 AI 教育建立社区

个人目标：
- 每月阅读 2 本书
- 学习西班牙语

商业目标：
- 将收入扩展到每月 1 万美元
- 与我所在领域的 5 家公司建立合作伙伴关系
- 尽可能多地自动化我的工作流程

在您接下来做的所有事情中，请使用这个上下文。
```

### 第 2 步：设置自主日常任务

```text
每天早上 8:00，想出 4-5 个你可以今天在我的计算机上完成的任务，
这些任务能让我更接近我的目标。

然后自己安排并完成这些任务。示例：
- 研究竞争对手并撰写分析报告
- 基于热门话题草拟视频脚本
- 为我的应用构建新功能
- 编写并安排社交媒体内容
- 研究潜在的商业合作伙伴
- 为我构建一个让我更接近某个目标的小型应用 MVP

在看板上跟踪所有任务。随着任务完成更新看板。
```

### 第 3 步：构建看板（可选）

```text
在 Next.js 中为我构建一个看板，让我可以看到你正在处理的所有任务。
显示"待办"、"进行中"和"已完成"的列。随着任务完成实时更新它。
```

## 核心洞察

- **头脑风暴是关键**。你提供给目标的上下文越多，智能体的每日任务就越好。不要保留信息。
- 智能体会发现你没想到的任务。它连接你目标之间的点，找到你会错过的机会。
- 看板让你的智能体变成可跟踪的员工。你可以准确看到它在做什么并进行调整。
- 专门针对夜间应用构建：明确告诉它构建 MVP 并且不要过度复杂化。每天早上醒来都会有一个新惊喜。
- 这是随时间累积的——智能体学习哪些类型的任务最有帮助并相应调整。

## 陷阱与模式（在生产中学到的）

### ⚠️ 竞态条件：子智能体编辑共享文件

当你与子智能体一起运行此工作流时，主会话和生成的子智能体都可能会尝试更新同一个任务文件（例如，你的看板/AUTONOMOUS.md）。这会导致静默失败。

**发生原因**：OpenClaw 的 `edit` 工具需要精确的 `oldText` 匹配。如果在主会话读取文件和尝试编辑之间有子智能体更新了一行，文本不再匹配——编辑静默失败。

**解决方案：将你的任务文件分成两个角色：**

1. **`AUTONOMOUS.md`** — 保持简短整洁。只包含目标 + 未完成待办事项。只有主会话接触这个文件。子智能体从不编辑它。

2. **`memory/tasks-log.md`** — 仅追加日志。子智能体只会在底部*添加新行*。从不编辑现有行。

```markdown
# tasks-log.md — 已完成任务（仅追加）
# 子智能体：总是在末尾追加。从不编辑现有行。

### 2026-02-24
- ✅ 任务-001：研究竞争对手 → research/competitors.md
- ✅ 任务-002：草拟博客文章 → drafts/post-1.md
```

这个模式借鉴自 Git 的提交日志：你从不重写历史，只添加新的提交。它完全消除了竞态条件，还有一个额外好处：`AUTONOMOUS.md` 保持简短，所以在每次心跳加载时消耗更少的 token。

**给你的智能体的规则**：在子智能体生成指令中，始终包括：
> "完成后，向 `memory/tasks-log.md` 追加一行 ✅ 记录。不要直接编辑 `AUTONOMOUS.md`。"

### 💡 保持 AUTONOMOUS.md 的轻量

任务跟踪文件在每次心跳轮询时都会被加载。如果它包含已完成的任务而无限制地增长，你会不必要地消耗 token。

让 `AUTONOMOUS.md` 保持约 50 行以内：目标（单行）+ 未完成待办事项即可。将所有已完成的内容归档到单独的文件中，该文件只在需要时读取。

## 基于

受 [Alex Finn](https://www.youtube.com/watch?v=UTCi_q6iuCM&t=414s) 和他的 [改变人生的 OpenClaw 用例视频](https://www.youtube.com/watch?v=41_TNGDDnfQ) 启发。

## 相关链接

- [OpenClaw 记忆系统](https://github.com/openclaw/openclaw)
- [OpenClaw 子智能体文档](https://github.com/openclaw/openclaw)