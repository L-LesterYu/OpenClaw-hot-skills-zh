# DenchClaw 本地 CRM 框架

用 OpenClaw 设置一个真正可用的 CRM 是一件痛苦的事情。你需要连接数据库、构建 UI、配置浏览器自动化、集成消息平台，还要让智能体理解你的数据架构。大多数人在中途放弃，最终得到一个半功能的 Notion 集成。

DenchClaw 是一个开源框架，它将 OpenClaw 变成一个完全本地的 CRM、销售自动化和生产力平台——通过一个命令安装并在你的机器上完全运行。

## 痛点

OpenClaw 作为底层功能非常强大，但将其用于实际的业务工作流程（潜在客户跟踪、主动营销、销售管道管理）需要将十几个工具拼接在一起：数据库、UI、浏览器访问、消息集成、文件管理。每个新集成都意味着更多的手动设置、需要管理的凭证以及可能出现故障的组件。你希望为业务操作获得 Cursor 级别的用户体验，而不是一堆 shell 脚本。

## 功能描述

- **一键安装**：`npx denchclaw` 安装所有内容——DuckDB 数据库、Web UI、OpenClaw 配置文件、浏览器自动化、技能——并在 `localhost:3100` 打开
- **自然语言 CRM**：询问"显示员工超过5人的公司"，它会实时更新视图。无需手动筛选
- **完整浏览器自动化**：复制你的 Chrome 配置文件，让智能体拥有相同的认证状态——说"从我的 HubSpot 导入所有内容"，它会登录、导出并导入
- **多种视图**：表格、看板、日历、时间线（甘特图）、画廊和列表视图——所有视图都可以通过 YAML 由智能体配置
- **应用构建器**：OpenClaw 构建独立的 Web 应用程序（仪表板、工具、游戏），它们在您的工作空间内运行并可访问您的数据
- **文件系统优先**：表格筛选器、视图、列切换、日历设置——所有内容都是文件，所以 OpenClaw 可以直接读取和修改
- **作为编码智能体工作**：DenchClaw 由 DenchClaw 构建。它也是您 Mac 的完整文件树浏览器和代码编辑器

## 如何设置

1. 通过一个命令安装：

```bash
npx denchclaw
```

2. 完成引导向导。DenchClaw 会创建一个名为 `dench` 的专用 OpenClaw 配置文件，并在端口 19001 启动网关。

3. 在浏览器中打开 `localhost:3100`。在 Safari 上，将其添加到你的 Dock 中作为 PWA（渐进式 Web 应用）。

4. 开始与它对话：

```text
嘿，创建一个"潜在客户"对象，包含字段：姓名、邮箱、公司、状态（新/已联系/已筛选/已成交/已流失）和备注。导入我从 Apollo 下载的潜在客户 CSV 文件。
```

```text
显示所有状态为"已联系"的潜在客户，并按最后更新时间排序。切换到按状态分组的看板视图。
```

```text
去我的 LinkedIn，找到最近20个查看我资料的人，将他们添加为潜在客户，并丰富他们的公司信息。
```

```text
基于每个潜在客户公司的最近新闻，为状态为"新"的潜在客户撰写个性化的主动营销邮件。将草稿保存在新的"主动营销草稿"文档中。
```

5. 你也可以通过 Telegram 或任何已连接的消息平台使用它——在路上用手机管理你的销售管道。

## 所需技能

DenchClaw 自动捆绑所有必需的技能：

- **CRM 技能** — 基于 DuckDB 的结构化数据管理，包含对象、字段、条目和多种视图类型
- **应用构建器技能** — 构建运行在工作空间内的 Web 应用程序（仪表板、工具），可访问你的数据库
- **浏览器自动化技能** — 具有你现有 Chrome 认证状态的 Chromium 浏览器，用于网页抓取、导入和主动营销

无需额外的技能安装或配置。

## 核心洞察

- **文件系统 = 智能体原生 UI**：因为每个设置、筛选器和视图都存储为 YAML/Markdown 文件，OpenClaw 可以像编辑代码一样自然地修改 UI。无需 API 包装器。
- **DuckDB 是最佳选择**：体积最小、性能最高的嵌入式数据库，仍然支持完整的 SQL。无需服务器进程、无需凭证、无需网络——只是一个文件。
- **Chrome 配置文件克隆是超能力**：与其对抗 OAuth 流程和 API 速率限制，DenchClaw 会复制你浏览器的认证状态。智能体看到的就是你看到的，做你做的事情。
- **一个 `npx` 命令胜过周末的设置**：整个堆栈（数据库、Web UI、OpenClaw 配置文件、网关、浏览器、技能）会自动安装和配置。无需 Docker，无需环境文件，无需依赖地狱。

## 演示

[观看演示视频](https://www.youtube.com/watch?v=pfACTbc3Bh4#t=43)——展示从安装到使用自然语言管理实时 CRM 管道的完整工作流程。

## 相关链接

- [DenchClaw GitHub](https://github.com/DenchHQ/DenchClaw) — MIT 许可，开源
- [DenchClaw 网站](https://denchclaw.com)
- [Discord 社区](https://discord.gg/PDFXNVQj9n)
- [技能商店](https://skills.sh)